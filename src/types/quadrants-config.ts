export type Quadrant = "Q1" | "Q2" | "Q3" | "Q4"

export interface QuadrantConfig {
   id: Quadrant
   label: string
   subtitle: string
   emoji: string
   color: string
   bgColor: string
   borderColor: string
   textColor: string
   badgeColor: string
}
